test_page: spec/index.html
before_tests: coffee -o dist/ -c src/tiler.coffee && coffee -o .tmp/ -c spec/tiler_spec.coffee && bundle exec sass src/tiler.sass dist/tiler.css && bundle exec sass spec/tiler_spec.sass .tmp/tiler_spec.css
src_files:
  - src/tiler.coffee
  - src/tiler.sass
  - spec/tiler_spec.coffee
  - spec/tiler_spec.sass
  - spec/index.html
serve_files:
  - .tmp/*.js
  - dist/*.js
launch_in_dev:
  - phantomjs
launch_in_ci:
  - phantomjs
